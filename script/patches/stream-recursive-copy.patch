diff --git a/crates/fs/src/fs.rs b/crates/fs/src/fs.rs
index 7bec1f8..2d93034 100644
--- a/crates/fs/src/fs.rs
+++ b/crates/fs/src/fs.rs
@@ -3418,38 +3418,61 @@ pub async fn copy_recursive<'a>(
     target: &'a Path,
     options: CopyOptions,
 ) -> Result<()> {
-    for (item, is_dir) in read_dir_items(fs, source).await? {
-        let Ok(item_relative_path) = item.strip_prefix(source) else {
-            continue;
-        };
-        let target_item = if item_relative_path == Path::new("") {
-            target.to_path_buf()
-        } else {
-            target.join(item_relative_path)
-        };
-        if is_dir {
-            if !options.overwrite && fs.metadata(&target_item).await.is_ok_and(|m| m.is_some()) {
-                if options.ignore_if_exists {
-                    continue;
-                } else {
-                    anyhow::bail!("{target_item:?} already exists");
-                }
+    copy_recursive_streaming(fs, source, target, options).await
+}
+
+fn copy_recursive_streaming<'a>(
+    fs: &'a dyn Fs,
+    source: &'a Path,
+    target: &'a Path,
+    options: CopyOptions,
+) -> BoxFuture<'a, Result<()>> {
+    use futures::future::FutureExt;
+
+    async move {
+        let metadata = fs
+            .metadata(source)
+            .await?
+            .with_context(|| format!("path does not exist: {source:?}"))?;
+
+        if !metadata.is_dir {
+            return fs.copy_file(source, target, options).await;
+        }
+
+        let target_exists = fs.metadata(target).await.is_ok_and(|metadata| metadata.is_some());
+        if target_exists && !options.overwrite {
+            if !options.ignore_if_exists {
+                anyhow::bail!("{target:?} already exists");
             }
-            let _ = fs
-                .remove_dir(
-                    &target_item,
+        } else {
+            if target_exists {
+                fs.remove_dir(
+                    target,
                     RemoveOptions {
                         recursive: true,
                         ignore_if_not_exists: true,
                     },
                 )
-                .await;
-            fs.create_dir(&target_item).await?;
-        } else {
-            fs.copy_file(&item, &target_item, options).await?;
+                .await?;
+            }
+            fs.create_dir(target).await?;
         }
+
+        let mut children = fs.read_dir(source).await?;
+        while let Some(child_path) = children.next().await {
+            let Ok(child_path) = child_path else {
+                continue;
+            };
+            let Some(file_name) = child_path.file_name() else {
+                continue;
+            };
+            let child_target = target.join(file_name);
+            copy_recursive_streaming(fs, &child_path, &child_target, options).await?;
+        }
+
+        Ok(())
     }
-    Ok(())
+    .boxed()
 }
 
 /// Recursively reads all of the paths in the given directory.
